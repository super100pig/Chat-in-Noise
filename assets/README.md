Place the processed real-world background loop here as:

```text
background-loop.mp3
```

The page loads `assets/background-loop.mp3`, loops it continuously after the first user interaction, and keeps its volume fixed from the UI.
